{
  "name": "lazydesigner-desktop",
  "private": true,
  "version": "0.1.0",
  "license": "GPL-3.0-only",
  "type": "module",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "dev:frontend": "vite --host 127.0.0.1 --port 1421 --strictPort",
    "prepare:icons": "tauri icon src-tauri/app-icon.svg",
    "prepare:managed-resource": "powershell.exe -NoLogo -NoProfile -ExecutionPolicy Bypass -File ./scripts/prepare-managed-resource.ps1",
    "dev:app": "npm run prepare:icons && tauri dev",
    "build:frontend": "vite build",
    "build:app:shell": "npm run prepare:icons && tauri build",
    "build:app": "npm run prepare:icons && npm run prepare:managed-resource && tauri build --config src-tauri/tauri.bundle.conf.json",
    "build:app:trusted": "npm run prepare:icons && npm run prepare:managed-resource && tauri build --config src-tauri/tauri.release.conf.json",
    "typecheck": "svelte-check --tsconfig ./tsconfig.json",
    "check:rust": "npm run prepare:icons && cargo check --locked --manifest-path src-tauri/Cargo.toml",
    "verify:source": "npm run typecheck && npm run build:frontend && npm run prepare:icons && cargo check --locked --manifest-path src-tauri/Cargo.toml && cargo test --locked --manifest-path src-tauri/Cargo.toml"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^6.2.4",
    "@tailwindcss/vite": "^4.3.2",
    "@tauri-apps/cli": "^2.0.0",
    "@types/node": "^24.0.0",
    "svelte": "^5.56.8",
    "svelte-check": "^4.3.0",
    "tailwindcss": "^4.3.2",
    "typescript": "^5.0.0",
    "vite": "^7.0.0"
  }
}
