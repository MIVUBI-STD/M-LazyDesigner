# 05 — Operations

Owns current development/validation state and local/native acceptance procedure.

```text
current-validation.md
next-action.md
local-acceptance-runbook.md
native-acceptance.md
```

These files are operational state, not durable architecture authority.

AI load rule: read `next-action.md` only when continuing current repository work; read `current-validation.md` only when interpreting proof/status; read `local-acceptance-runbook.md` when executing local code acceptance; read `native-acceptance.md` only when proving the exact installed Gateway/Runtime/plugin/Blockbench chain after a fresh install or material runtime/authoring-intelligence change.